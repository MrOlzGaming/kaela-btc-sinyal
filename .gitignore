node_modules/
.netlify/

# Hash PIN konfirmasi "beneran Olan" (tools/confirm-olan.ps1, 6 Sep 2026) -- repo ini PUBLIC,
# commit hash PIN pendek/numerik ke sini = bisa di-brute-force offline siapapun yang clone. Yang
# dicommit CUMA skrip-nya (logic), hash-nya WAJIB lokal doang. Token approval juga lokal/transient
# (1 baris timestamp, sekali pakai, dihapus abis dibaca) -- gak ada gunanya buat siapapun tapi
# tetap gak perlu numpuk di git history.
tools/.kaela-confirm-secret.hash
.kaela-confirm/

# cache candle mentah dari Binance -- regeneratable via backtest/fetchKlines.js, terlalu besar buat di-commit
backtest/*-cache.json
backtest/yahoo-btc-*.json

# hasil sweep/backtest mentah -- regeneratable via node backtest/*.js, bukan source of truth
backtest/*-result.json

# state runtime lokal (posisi terbuka, candle terakhir diproses) -- bukan source, beda tiap mesin
# (CI tetap commit ini via `git add -f`, cuma disembunyiin dari git status LOKAL biar testing
# manual gak numpuk noise "M ...state.json" tiap kali dites)
state.json
nyopet-state.json
whale-state.json
sniper-trigger-state.json
price-alert-state.json
squeeze-alert-state.json
econ-calendar-notified.json
conviction-track-record.json
analyst-dashboard.json
anomaly-history.json
nyopet-otp-state.json
usd-idr-rate-cache.json

# hasil riset/sweep sesi lokal (bukan "-cache"/"-result" tapi tetep regeneratable dari script
# backtest/*.js, terlalu besar buat di-commit) -- 22 Agu 2026
backtest/btc-synthetic-future.json
backtest/correlation-raw.json

# file lepas, bukan bagian source
*.jpeg
*.jpg
*.png
!web/icons/*

# kredensial -- JANGAN PERNAH commit. Tanpa file ini, fonnte.js otomatis no-op (aman buat dev).
secrets.js

# PAT GitHub buat push manual ke origin-new (MrOlzGaming) dari sesi lokal -- JANGAN PERNAH commit.
.gh-token-mrolzgaming
local-executor.log
force-sync-check.log
manual-open-check.log
.KAELA-TRASH/

# Data trading PERSONAL member Kaela Pro Trader (23 Agu 2026) -- repo ini PUBLIC, data member
# (entry/SL/TP/PNL pribadi) TIDAK BOLEH ke situ walau gak ada secret di dalamnya. Sumber kebenaran
# aslinya tetap Sheet "Journal" di GAS (APPS/kaela-multi-akun/) -- file lokal ini cuma state kerja.
multi-account-state/

# liquidationListener.js (12 Sep 2026) -- data runtime proses persisten, beda tiap mesin/waktu,
# regeneratable dari feed live -- bukan source of truth, gak perlu numpuk di git history.
liquidation-heatmap.json
liquidation-events.jsonl
liquidation-listener.log

# Riset kalender ekonomi (12 Sep 2026) -- arsip MURNI LOKAL (econReactionResearchLog.js), sama
# alasan kayak state file lain di atas.
econ-reaction-research-log.json
econ-calendar-live-notified.json
vultr-balance-monitor-state.json
smart-money-divergence-state.json
walletexplorer-cache.json
