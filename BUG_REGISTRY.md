# 🐛 BUG_REGISTRY.md — Kaela BTC Sinyal

Catatan formal bug NYATA yang ketemu+fix di sistem ini -- diadaptasi dari pola **P-14 Bug
Registry** punya `NEXUS-FORGE` (`D:\KAELA PROJECT\NEXUS-FORGE\`, kerangka tim-AI Olan yang selama
ini jalan manual lintas sesi chat terpisah). Di sini disederhanakan buat konteks 1 sesi
terus-menerus (Claude Code) -- gak pakai dispatch/banner/role-switch, cuma bagian yang beneran
kepake: **1 baris = 1 bug, gampang dicari, gak nyebar di prosa memori**.

Kenapa dibikin (13 Sep 2026): bug KRITIS tranId-dedup (lihat BUG-KAELATRADE-0001) mencemari data
9 hari sebelum ketauan, dan sebelum ini gak ada satupun tempat terpusat buat liat "bug apa aja
yang pernah kejadian di sini, akar masalahnya apa, udah beneran ketutup apa belum". Riwayatnya
ada, tapi nyebar di comment file + memori Claude -- registry ini SATU tempat ringkas, git-tracked,
kebaca siapapun (termasuk sesi Claude baru) tanpa harus baca ulang seluruh histori chat.

**Format**: `BUG-KAELATRADE-[NNNN]` (nomor urut, gak pernah dipakai ulang walau bug lama dihapus
dari daftar aktif -- diarsipkan di bawah, bukan dihapus).

## Aktif / Baru Ditutup

| ID | Tanggal | Ringkasan | Root Cause | Fix (commit) | Regression Test | Status |
|----|---------|-----------|-------------|---------------|------------------|--------|
| BUG-KAELATRADE-0001 | 2026-09-13 | **KRITIS** — "PnL hari ini" salah total (Binance app +$12,96, sistem bilang -$1,89) | `tradeHistoryStore.js` dedup pakai `id: String(tranId)` polos — Binance kasih `tranId` SAMA buat `REALIZED_PNL` + `COMMISSION` dari 1 fill, entry COMMISSION (selalu muncul belakangan di response) NIMPA REALIZED_PNL. Mencemari data sejak file dibuat (4 Sep) — 9 hari. Bug IDENTIK ditemukan independen di `multiAccountExecutor.js` (implementasi sync terpisah, sebelum dikonsolidasi) | `44baaa2` (tradeHistoryStore.js), `e451509` (multiAccountExecutor.js, URGENT — nyaris re-korup data yang baru direbuild) | `regressionTests.js` (test #1-3, ground truth `+12.96136192`) | ✅ CLOSED |
| BUG-KAELATRADE-0002 | 2026-09-08 | Whale-scan cuma proses ~8/144 blok/hari, backlog numpuk (~211 blok/1,5 hari) | `whaleDailyDigest.js` gerbang scan blok DI BELAKANG `hasEntryToday('whale-daily')` (cek "udah kirim WA hari ini?") — scan blok baru jalan kalau BELUM kirim WA, padahal WA cuma 1x/hari | `2a6e955` — scan didekopel dari gerbang kirim-WA, jalan tiap cycle | — (belum ada, kandidat solusi #3 lanjutan) | ✅ CLOSED |
| BUG-KAELATRADE-0003 | 2026-09-13 | Pesan posisi (tambah/tutup) ke grup Wibowo Hedgefund hilang PERMANEN kalau cek toggle broadcast gagal | `kaelaProTraderClient.js` `getWibowoBroadcastEnabled()` — kalau GAS check-nya gagal/timeout (bukan gagal KIRIM, gagal CEK IZIN kirim), pesan dianggap "gak boleh kirim" dan dibuang, TANPA retry | `9b92735` (fix awal), `518d901` (antrean retry `wibowo-broadcast-queue.json`, khusus utk kegagalan CEK, beda dari retry-safety `fonnte.js` yang udah ada utk kegagalan KIRIM) | — | ✅ CLOSED |
| BUG-KAELATRADE-0004 | 2026-09-13 | Pesan "Tutup Posisi (gak ke-track)" NUDUH "kemungkinan besar manual trading" sbg satu-satunya dugaan, padahal histori bug proyek ini (insiden 3/8/12 Sep) nunjukin penyebab SAMA MUNGKINNYA (bahkan lebih sering) adalah mesin eksekutor pindah leader (journal lokal gak ke-sync, `multi-account-state/` sengaja gak lewat git) — bisa nyalahin Olan buat hal yang bukan aksinya | `formatAutoClosedUntracked` (`darkKaelaLog.js`) nulis kalimat sebab-akibat pasti padahal sistem gak pernah beneran tau mana dari 2 kemungkinan yang kejadian | `e1cd589` | — | ✅ CLOSED |
| BUG-KAELATRADE-0005a | 2026-09-14 | **KRITIS** — `run-vultr-executor.sh` skip TOTAL 10 siklus berturut-turut (6+ jam, 19:32-02:00 WITA), NOL siklus 15-menit jalan. Ketauan dari GEJALA gak langsung: Olan lapor "PnL Harian 0%" di web (data pool gak ke-update, bukan pasar diem) | ⚠️ DIAGNOSIS AWAL SALAH — dikira `node` script HANG (18/26 panggilan emang gak ada `timeout`, itu tetap dibenerin sbg hardening, TAPI itu BUKAN sebab insiden ini spesifik). Root cause ASLI: `run-position-check-fast.sh` (tiap 5 menit) & `run-vultr-executor.sh` (tiap 15 menit) BERBAGI lock yang SAMA by design (nulis state file yang sama) — crontab `*/5` bikin script 5-menitan PERSIS nembak di menit :00/:15/:30/:45, SAMA kayak yang 15-menitan. Karena preamble-nya jauh lebih pendek (langsung ke `flock`, gak ada git-sync/heartbeat dulu), script 5-menitan SELALU menang rebutan lock -- yang 15-menitan KALAH TERUS di SETIAP siklus, bukan sesekali. `positionCheckFast.js` SENDIRI terbukti sehat (log jalan mulus tanpa jeda) -- itu yang bikin `lsof`/`ps aux` gak pernah nemu proses "nyangkut" pas dicek manual (emang gak ada yang hang beneran) | `5a90110` (hardening timeout, tetap berguna buat hang GENUINE ke depan, plus `checkExecutorStuck.js` pengawas) + crontab `run-position-check-fast.sh` digeser `*/5`→`1-59/5` (gak pernah lagi ketemu :00/:15/:30/:45) -- fix SEBENARNYA buat insiden spesifik ini, didokumentasikan di komentar `run-position-check-fast.sh` biar redeploy VPS ke depan gak balik ke `*/5` lagi | `regressionTests.js` (test `detectStuck`, tapi TES ITU nguji gejala/deteksi, BUKAN root cause scheduling race -- belum ada test buat race ini sendiri) | ✅ CLOSED |

| BUG-KAELATRADE-0006 | 2026-09-14 | **KRITIS** — `mexcExecutor.js` `placeMarketEntry` gagal TOTAL ("Leverage multiplier must be within the upper limit 100 and lower limit 1") -- MEXC nolak SEMUA order buka posisi | Endpoint `order/create` MEXC TERNYATA wajib field `leverage` eksplisit di body, beda dari Binance yang cukup diset sekali via endpoint terpisah (`change_leverage`) dan otomatis kepake buat order berikutnya. `mexcExecutor.js` gak pernah nyertain field ini -- BELUM PERNAH KETAUAN karena integrasi ini emang belum pernah dites pakai API key beneran sebelum hari ini (30 Agu 2026 ditulis dari dokumentasi doang) | `92f13b9` — cache leverage TERAKHIR yang diset per symbol (dari `setLeverage`), otomatis disertain `placeMarketEntry` -- interface caller (Sniper/Nyopet) TETAP SAMA, gak perlu ubah | Manual (tes live, belum ada regression test otomatis -- perlu mock HTTP MEXC dulu) | ✅ CLOSED |
| BUG-KAELATRADE-0007 | 2026-09-14 | **KRITIS/BAHAYA** — `emergencyCloseMarket`+`placeStopLoss`+`placeTakeProfit` MEXC pakai kode arah TERBALIK (`closeSide`) -- tutup posisi LONG gagal "Position is nonexistent or closed". Kalau gak ketemu, SEMUA posisi Emas MEXC auto-trading bakal kebuka TANPA Stop Loss/Take Profit sama sekali (SL/TP-nya pasti ditolak MEXC dengan pola bug yang sama) | Kode side MEXC: 2=close SHORT, 4=close LONG (dikonfirmasi dokumentasi) -- versi lama nulis `direction==='buy' ? 2 : 4` (PERSIS TERBALIK). Ini SEBELUMNYA cuma "CATATAN belum diverifikasi, TODO cek pas ada API key" di komentar kode -- gak pernah beneran dites sampai hari ini | `92f13b9` — dibalik jadi `direction==='buy' ? 4 : 2` di ketiga fungsi. Diverifikasi LANGSUNG: tutup LONG sukses, buka SHORT+pasang SL/TP sukses (dikonfirmasi via app MEXC Olan -- "Open Orders(2)" muncul, sebelumnya bakal 0/error). Logic dikonsolidasi jadi `closeSideFor()` di `c3d20e6` (lihat BUG-0009) | Manual (tes live) + `regressionTests.js` test `closeSideFor` (sejak konsolidasi `c3d20e6`) | ✅ CLOSED |
| BUG-KAELATRADE-0008 | 2026-09-14 | Pesan posisi manual (Buka/Tutup/Add/Reduce/Flip/HiddenActivity) buat symbol MEXC bakal nampilin ticker MENTAH exchange (`PAXG_USDC`/`XAUT_USDT`) ke grup Wibowo Hedgefund -- pelanggaran aturan lama [[feedback-ticker-nama-literal]] ("Emas JANGAN literal token MEXC, Olan bingung"). Ketauan+dibenerin SEBELUM pesan salah sempat kekirim (untung siklus deteksinya kena skip lock beberapa kali duluan) | `positionReconciler.js` SENGAJA didesain "gak kenal" symbol (biar asset APAPUN yang Olan trading manual tetap kelaporan) -- desain itu benar, TAPI efek sampingnya symbol MENTAH ikut kebawa ke pesan tanpa di-translate ke label yang biasa dilihat Olan | `92f13b9` — lookup KECIL simbol->label dari asset config Sniper+Nyopet yang UDAH ADA (`nyopetAssetConfig.js`/`assetConfig.js`), symbol yang BENERAN gak dikenal (asset di luar BTC/Emas) tetap tampil apa adanya, desain awal ("asset apapun") gak rusak | Manual (tes live, render preview `formatManualOpen` dicek manual sebelum+sesudah fix) | ✅ CLOSED |

| BUG-KAELATRADE-0010 | 2026-09-19 | **KRITIS/DUIT** — Pesan "hasil" FOMC bilang "NETRAL" padahal The Fed BENERAN naikkan suku bunga 25bps -- Olan ambil keputusan trading MANUAL di platform lain (BC.Game) berdasar pesan itu, RUGI beneran | `concludeHawkishDovish()` (`econCalendarLog.js`) label "NETRAL" artinya "actual PERSIS SAMA forecast" (gak ada kejutan), BUKAN "gak ada perubahan" -- pembaca awam (termasuk Olan sendiri) wajar salah paham baca "NETRAL" = "gak kejadian apa-apa". Diperparah data "actual" dari feed gratis (ForexFactory) sering telat/kosong di jendela reaksi sempit (~10-15 menit) econCalendarLiveMonitor.js -- arsip lokal (`econ-calendar-headsup`/`econ-calendar-result` di `archive.json`) TERNYATA NOL entri sepanjang sejarah fitur ini (5 Sep-19 Sep), gak ada rekam jejak buat verifikasi persis pesan mana yang kepicu | `93e8fbf` -- pesan "HASIL" hawkish/dovish/netral (`formatResult`/`formatResultExplanation`/`formatResultFollowup`) DIHAPUS TOTAL, sisa pesan HEADS-UP (info siap-siap, TANPA klaim arah) doang. Sinyal trading OTOMATIS Kaela (scalp econ_reaction) TIDAK kepengaruh -- itu dari reaksi harga BTC asli, bukan label ini | — (perubahan hapus pesan, bukan logic kalkulasi -- gak ada angka ground-truth buat di-assert) | ✅ CLOSED |
| BUG-KAELATRADE-0011 | 2026-09-19 | (bukan bug, KEBIJAKAN BARU susulan BUG-0010) -- Olan putuskan stop trading manual PERMANEN di akun real sendiri ("trading 100% ku serahkan ke Kaela, posisi non-Kaela boleh auto-close") | N/A -- keputusan Olan, bukan akar masalah teknis | `b369201`/`93ad5f5` (binanceExecutor.js `wasLastEntryOrderByKaela` + `newClientOrderId`), MEXC `externalOid` versi sama (`positionReconciler.js`) -- posisi yang kedetek PASTI bukan order Kaela (dicek clientOrderId/externalOid LANGSUNG ke exchange, bukan tebakan jurnal lokal) di-auto-close SEGERA; kalau gak bisa dipastikan, default AMAN (lapor doang, JANGAN tutup) | `regressionTests.js` (`formatManualOpenAutoClosed` -- PnL null vs numerik) | ✅ CLOSED |
| BUG-KAELATRADE-0034 | 2026-09-19/20 | Susulan BUG-0033 — Olan kirim screenshot dashboard Kaela Access (`kaela-access.netlify.app`): grid riwayat posisi tertutup padat tanpa tanggal per-kartu sama sekali ("riwayat trading amburadul dan ga tau trade tanggal brapa") | Root cause GANDA: (a) `renderOrderCard` (Sniper) dan `renderNyopetOrderCard` (Nyopet) di `kaela-render.js` — dipakai BERSAMA oleh web Trading Engine sendiri DAN Kaela Access via CDN jsDelivr — template kartu CLOSED gak pernah nyisipin baris tanggal tutup di luar `partialTimeline` (yang sendirinya digerbang `o.partialDone`), jadi kartu tanpa partial (mayoritas) nol info tanggal; (b) Journal sheet GAS ("Kaela Access") gak pernah punya mekanisme bersih-bersih — 50 baris numpuk (47 closed) sejak fitur ada, sama sekali gak match sama kondisi "fresh" yang diinginkan Olan | `435ed1a1` (kaela-btc-sinyal) — tambah `closedDateLine` (pakai `fmtOpenedDate`+key i18n baru `closed_at`) di KEDUA closed-card template, unconditional (gak digerbang partial); CDN jsDelivr di-purge manual, diverifikasi `curl` konten CDN abis purge udah ke-update. Lalu (kaela-multi-akun, GAS v226) — fungsi baru `adminArchiveClosedJournal` (`Journal.gs`, SERVICE_KEY-gated): salin SEMUA baris `Status==='closed'` ke sheet baru `_ARCHIVE_Journal_<timestamp>` (pola sama `Pool.gs` reset lama), baru bersihin dari Journal aktif — baris `open`/`failed` SENGAJA gak disentuh (posisi lagi jalan, kalau EntryId ilang `updateJournalEntry` pas nutup nanti gagal nyatet ClosedAt/PnlUsd). Dijalankan sekali manual 20 Sep: 47 diarsip ke `_ARCHIVE_Journal_20260920_001155`, 3 open tetap utuh | Diverifikasi mock browser JS console (closedDateLine muncul benar format "🏁 Ditutup DD/MM/YYYY HH:MM WITA") + verifikasi live: `dumpJournalEntries` post-archive nunjukin PERSIS 3 baris tersisa (semua status open, EntryId cocok posisi yang beneran masih jalan) | ✅ CLOSED |
| BUG-KAELATRADE-0033 | 2026-09-19 | Web jurnal.html/kaela-render.js: (a) kolom "Mode" riwayat Nyopet nampilin kode mentah ("wedge_rising" dst) buat 4 dari 8 kemungkinan pola, (b) saldo Nyopet gak nyebut modal aktif (1/5 saldo penuh) yang beneran dipakai sizing, (c) teks pembuka overclaim "grafik...dari waktu ke waktu" padahal equity curve yang ada itu per-trade — Olan: "riwayat di jurnal amburadul" | (a) `NYOPET_MODE_LABEL_WEB` cuma nge-map versi bull/falling (`flag_bull`/`pennant_bull`/`wedge_falling`/`fvg_bounce`), 4 versi bear/rising (`flag_bear`/`pennant_bear`/`wedge_rising`/`fvg_bounce_bear`, lihat `chartPatterns.js` baris ~126-137) gak pernah ditambahin — persis pola short window-bear yang belakangan makin sering; (b) `MODAL_ACTIVE_FRACTION` (1/5) cuma disebut di Kalkulator, gak ditempel di halaman Jurnal sendiri; (c) `renderEquityCurveSvg` itu asli ADA (bukan gak dibangun sama sekali — audit sebelumnya di hari yang sama SEMPAT salah bilang "nol implementasi" krn grep cuma cari kata "canvas/Chart", padahal fungsinya dinamain "Curve" — dikoreksi begitu ketauan), tapi x-axis-nya urutan TRADE bukan kalender, gak match kata "dari waktu ke waktu" | `1aba21ce` — (a) 4 label ditambahin ngikutin gaya existing per keluarga pola; (b) baris "modal aktif: $X" ditempel di bawah tiap label saldo; (c) wording diganti "equity curve per-trade (muncul begitu minimal 2 trade selesai)" | Diverifikasi langsung di browser (bukan cuma baca kode) — 4 label resolve benar, modal aktif $1.000 muncul, wording baru tampil. `regressionTests.js` 17/17 tetap lolos (murni web/JS) | ✅ CLOSED |
| BUG-KAELATRADE-0032 | 2026-09-19 | **REGRESI dari fix hari ini sendiri** — `sniperAutoAnalysis.js` `ReferenceError: roleOpener is not defined`, crash TOTAL (`main()` reject, exit 1) begitu ada order status bukan `'floating'` — ketemu lewat REVIEW INDEPENDEN (bukan audit sendiri) setelah user eksplisit nanya "beneran udah bersih?" | Fix BUG-0031 item (d) (alert "pending stuck") manggil `roleOpener('DRAKE', ...)` tapi lupa `require('./teamRoles')` -- `node -c` gak nangkep ini karena ini runtime `ReferenceError`, bukan syntax error, dan `regressionTests.js` gak ada test yang mensimulasikan order status non-floating | `8e814860` — tambah `require('./teamRoles')`. Diverifikasi dengan reproduksi PERSIS baris yang crash (bukan cuma baca kode) -- konfirmasi pesan sekarang terbentuk normal | Manual (reproduksi langsung baris yang crash sebelum+sesudah fix, `node -c` + `regressionTests.js` 17/17) | ✅ CLOSED |
| BUG-KAELATRADE-0031 | 2026-09-19 | Item "C" tersisa dari audit sistem (5 titik kecil, gak mendesak tapi konkret): (a) `pnlCrossCheckMonitor.js` kirim gagal tanpa jejak, (b) `run-vultr-executor.sh` reset --hard tanpa cek apa yang dibuang, (c) `rsi()` overbought palsu buat harga flat total, (d) hari "pending stuck" Sniper diam total tanpa penjelasan, (e) 2 implementasi `hitung()` (server+client) rawan divergen diam-diam | (a) gak ada cek hasil `sendWhatsApp`; (b) `git reset --hard origin-new/master` gak pernah dicek dulu apa ada working-tree kotor/commit lokal belum ke-push yang bakal ke-buang; (c) `avgLoss===0` gak bedain avgGain juga 0 (flat total) vs avgGain>0 (beneran semua naik); (d) `return` awal kalau ada order status!=='floating' gak pernah lapor apapun; (e) literal arah `'sell'`(server)/`'short'`(client) beda tanpa penanda silang | `a41e1e55` — (a) log "GAGAL" eksplisit (ke-scan CYCLE_ERRORS existing); (b) cek `git status --porcelain`+`rev-list --count` sebelum reset, log peringatan kalau ada yang bakal hilang; (c) `avgGain===0 && avgLoss===0` return 50; (d) `addOrReplaceDaily('sniper-pending-stuck', ...)` + WA 1x/hari jelasin akar masalah; (e) komentar silang di kedua file | `regressionTests.js` 17/17 tetap lolos; `rsi()` dites manual (flat->50, naik->100) | ✅ CLOSED |
| BUG-KAELATRADE-0030 | 2026-09-19 | Lanjutan audit sistem -- 3 gap monitoring + 1 invariant generik: (a) dedup ultah gak pernah ketutup mandor, (b) nol health-check buat pipeline `econCalendarLiveMonitor.js`, (c) nol pengawasan error `run-econ-calendar-live-vultr.sh` sendiri, (d) BUG-0012 masih bisa kejadian lagi di modul LAIN tanpa ketahuan | (a) `checkNoDuplicateSpam` cuma exact-match `DAILY_DEDUP_TYPES`, `birthdayGreeting.js` nulis type dinamis `birthday-<key>`; (b) `dailyAutomationChecklist.js` gak punya `checkXFreshness` buat econ-calendar-live padahal ini pipeline yang baru terbukti (BUG-0010) berdampak finansial nyata; (c) `run-econ-calendar-live-vultr.sh` gak pernah panggil `reportCycleErrors.js` beda dari 2 pipeline cron lain; (d) `systemInvariantCheck.js` cuma cek jurnal, gak ada invariant generik "trigger-state bilang done tapi archive nol entri" | `ac54a0ec` — (a) `DAILY_DEDUP_PREFIXES`; (b) `checkEconCalendarFreshness()` (pola sama `checkWhaleScanFreshness`, mtime state file vs ambang 20 menit); (c) `reportCycleErrors.js` ditambah di ujung siklus; (d) `TRIGGER_STATE_CHECKS` array data-driven + `checkTriggerStateVsArchive()` di `systemInvariantCheck.js` — nutup KELAS BUG-0012 permanen, bukan cuma instance Sniper. Juga: GAS `kaela-multi-akun` `HEARTBEAT_STALE_AFTER_MINUTES` (20->40, item tertunda audit sebelumnya) di-deploy live versi 225 | `regressionTests.js` 17/17 tetap lolos; `systemInvariantCheck.js` dites manual (data real: 0 anomali baru; simulasi tanggal fiktif: terdeteksi benar) | ✅ CLOSED |
| BUG-KAELATRADE-0021 | 2026-09-19 | **KRITIS** — instance Nyopet DEFAULT (`_defaultTrader`, akun Olan sendiri lewat `run-vultr-executor.sh`) permanen `isDemo=true` & `baseUrl=demo-fapi` apapun status kill switch beneran — ketemu lewat audit menyeluruh sistem (4 sub-agent paralel) | `apiCreds` cuma diisi eksplisit oleh caller multi-akun (`testnet: account.mode==='demo'`) — instance default (`createNyopetTrader({onEvent, sendWA})` polos, TANPA `apiCreds`) gak pernah dapet nilai itu, jatuhnya SELALU `testnet=true` diam-diam, gak peduli `live-trading-config.json` yang sebenarnya | `794ffecc` — fallback ke `killSwitch.isTestnet()` (`isTestnetGlobal()`) kalau `apiCreds.testnet` gak eksplisit boolean; 2 titik `isInsufficientBalanceError` ikut dibenerin biar konsisten | `regressionTests.js` 17/17 tetap lolos (belum ada test spesifik — perilaku baru cuma kena kalau `killSwitch` di-flip mainnet, butuh tes live) | ✅ CLOSED |
| BUG-KAELATRADE-0022 | 2026-09-19 | Arsip `econ-calendar-headsup`/`econ-calendar-result` NOL entri sepanjang sejarah (5-19 Sep) — root cause ASLI ketemu, BUKAN cuma "data actual telat" seperti dicatat BUG-0010 | `run-econ-calendar-live-vultr.sh` (cron 5 menit) nulis `archive.json`/`econ-reaction-research-log.json` tapi TIDAK PERNAH commit sendiri — `run-vultr-executor.sh` (cron 15 menit TERPISAH) mulai tiap siklusnya dengan `git fetch && reset --hard`, rutin menghapus balik tulisan yang belum sempat ke-commit | `794ffecc` — script commit+push sendiri (retry 3x) segera setelah nulis. **KOREKSI (review independen, sesi sama)**: fix awal ini MEMPERSEMPIT jendela race dari "berjam-jam" jadi "beberapa detik" (antara node nulis archive.json dan git commit), BUKAN menghilangkan total seperti awalnya diklaim — `run-econ-calendar-live-vultr.sh` gak pakai `flock` yang sama kayak `run-vultr-executor.sh`. Susulan `8e814860`: bagian commit dibungkus `flock -w 5` di lock file SAMA (`/tmp/kaela-executor.lock`, fd beda), gagal ambil lock tetap lanjut (best-effort) — jendela race sekarang mendekati nol, TETAP bukan matematis nol (skenario ekstrem: reset --hard proses lain kejadian di detik yang sama pas lock lagi gagal diambil) | Manual (perlu observasi beberapa hari archive.json buat konfirmasi entri mulai muncul) | ✅ CLOSED (dengan catatan: risiko residual sangat kecil, bukan nol, didokumentasikan jujur) |
| BUG-KAELATRADE-0023 | 2026-09-19 | `nyopetJournalLock.js`: lock "basi" (>30dtk) bisa dicuri proses lain walau pemegang asli masih hidup (lambat, bukan crash) — `releaseLock` gak cek kepemilikan sebelum unlink | Proses A pegang lock, kerjanya (fetch candle paginasi+harga+waitForFill+WA, semua di 1 critical section) kebetulan >30dtk (jaringan lambat, pola BUG-0005a) tapi belum crash. Proses B anggap basi, hapus+bikin lock baru miliknya. A akhirnya selesai, `releaseLock()` (unlink polos) HAPUS lock milik B, bukan lock A yang udah dicuri — proses C bisa ikut masuk SEMENTARA B masih kerja | `794ffecc` — fencing token unik per-acquire, `releaseLock` cocokin token dulu sebelum unlink; ambang basi naik 30dtk->90dtk | Manual (skenario "lock dicuri" disimulasikan node -e ad-hoc, terverifikasi A gak hapus lock B) | ✅ CLOSED |
| BUG-KAELATRADE-0024 | 2026-09-19 | `mexcExecutor.js` `quantityToVol` pakai `Math.floor` — floating-point noise berisiko bikin fitur auto-close (posisi non-Kaela, BUG-0011) nyisain residual kontrak gak ketutup total di MEXC (Emas) | `quantity/contractSize` yang SEHARUSNYA integer persis (holdVol) bisa kena floating-point JS (mis. `9.999999999999998` bukan `10`) buat contractSize non-bulat — `Math.floor` pada kasus itu MEMBULATKAN KE BAWAH, vol yang dikirim ke `order/create` 1 kontrak lebih kecil dari posisi asli | `794ffecc` — `Math.round` (quantity di titik ini SELALU representasi vol integer yang udah eksis, beda dari `placeMarketEntry` yang emang butuh floor buat notional baru) | — (risiko floating-point spesifik, belum ketemu reproduksi pasti untuk contractSize MEXC yang dipakai — fix tetap strictly lebih aman) | ✅ CLOSED |
| BUG-KAELATRADE-0025 | 2026-09-19 | `sniperAutoAnalysis.js`: gerbang `!anyNewSignal` (global lintas-aset) buang status "gagal/gak ada pola" 1 aset total begitu ASET LAIN dapat sinyal valid hari yang sama | `anyNewSignal` cuma disetel true oleh candidate valid ASET MANAPUN, tapi ikut men-gate pesan invalid gabungan buat SEMUA aset — bertentangan sama invarian terdokumentasi sendiri ("tiap aset WAJIB dapat minimal 1 baris status per hari") | `794ffecc` — gerbang jadi per-laporan (`invalidNotes.length > 0` polos, gak peduli aset lain) | `regressionTests.js` 17/17 tetap lolos | ✅ CLOSED |
| BUG-KAELATRADE-0026 | 2026-09-19 | `dark-kaela-monitor.yml` (GH Actions, loop ~5,5 jam): journal Nyopet real (`nyopet-journal.json`) ditulis proses INI dan `run-vultr-executor.sh` (VPS, TERPISAH total) tanpa koordinasi — job cuma checkout SEKALI di awal, gak re-pull kecuali kebetulan ada perubahan buat push | Loop internal (`nyopetPositionMonitor.js`, tiap 5 menit) bisa baca journal yang udah BASI sampai berjam-jam vs tulisan Vultr yang independen — keputusan (TP/SL/likuidasi) bisa berbasis data usang | `794ffecc` — `git pull --rebase origin master` (gagal pun gak fatal, `rebase --abort` cleanup) tiap AWAL iterasi, bukan cuma pas mau push | Manual (butuh observasi live, gak ada unit test buat workflow YAML) | ✅ CLOSED |
| BUG-KAELATRADE-0027 | 2026-09-19 | Pola sistemik "arsip/dedup-state ditulis sebelum verifikasi `sendWhatsApp` sukses" di 8 titik lintas 7 file — kelas bug SAMA kayak BUG-0012 tapi lebih luas | `sendWhatsApp()`/`sendWhatsAppExcept()` SENGAJA gak pernah `throw` kalau Fonnte gagal (`fonnte.js`, biar 1 kegagalan WA gak gugurin seluruh run) — cuma balikin `{ok:false}`. Banyak caller nulis `addEntry`/`addOrReplaceDaily`/`saveState(...lastAlertAt)` SEBELUM/tanpa cek hasil itu -- gagal kirim = dianggap "udah kirim hari ini", gak dicoba ulang | `69535bb3` — `dailyAutomationChecklist.js` (laporan mandor sendiri), `checkExecutorStuck.js` (alert macet, PALING kritis — titik paling butuh notif justru paling rawan), `newsMonitor.js`, `anomalyScanner.js`, `priceAlertMonitor.js` (2 titik), `birthdayGreeting.js`, `dxyZoneMonitor.js` (3 titik), `groupMonitor.js` — semua kirim DULU, arsip cuma kalau sukses/skipped | `regressionTests.js` 17/17 tetap lolos | ✅ CLOSED |
| BUG-KAELATRADE-0028 | 2026-09-19 | Web (`kaela-btc-sinyal.netlify.app`): 2 bug UI ketemu audit — (a) `kalkulator.html` baris "Modal Aktif" selalu nongol kosong buat strategi Sniper walau harusnya disembunyikan; (b) `sniper-orders-widget.js` label durasi Indonesia ketuker (hari="h", detik="d") | (a) `.hidden{display:none}` didefinisikan SEBELUM `.result-row{display:flex}` di stylesheet sama, spesifisitas sama -> urutan menang; (b) singkatan `d`/`h` versi Indonesia ditulis KEBALIKAN dari konvensi h=jam/d=hari yang dipakai versi Inggris 2 baris di atasnya — posisi terbuka 6 hari 11 jam kebaca "6h 11j 2m 23d" | `69535bb3` — (a) compound selector `.result-row.hidden`; (b) ganti jadi `hr`/`j`/`m`/`dt` | Manual (diverifikasi langsung browser: Sniper->hidden, Nyopet->`$200` tampil benar) | ✅ CLOSED |
| BUG-KAELATRADE-0029 | 2026-09-19 | `backtestFlagBreakout.js`: `maxNyawaPct` gak pernah ditegakkan di 8 pemanggilan `runFlagBacktestWindowGated` resmi, padahal live (`sniperAutoAnalysis.js`) HARD-REJECT nyawa%>20 — angka riset yang dikutip buat izin short BTC live jadi kurang representatif | Parameter opsional `maxNyawaPct` default `null` (gak ada trade dibuang), tapi TIDAK ADA satupun call site resmi yang mengisinya | `69535bb3` — `maxNyawaPct: 20` (samain `MAX_NYAWA_PCT` sniperAutoAnalysis.js) di semua 8 titik. Angka Emas window-gated bergeser $3.139->$4.500 (kesimpulan arah TETAP: masih jauh kalah baseline $11.469) — komentar `sniperAutoAnalysis.js` diupdate | Backtest dijalankan ulang manual, angka baru dikonfirmasi | ✅ CLOSED |
| BUG-KAELATRADE-0012 | 2026-09-19 | Checklist harian Sniper (`dailyAutomationChecklist.js`) bisa lapor "done" walau `archive.json` NOL entri hari itu -- ketemu pas audit pesan WA sejak 14 Sep | Dua gap nempel: (1) 3 jalur short window-bear (`sniperAutoAnalysis.js` -- BTC auto-exec, info-only non-BTC, Sniper-4H) kirim WA sukses tapi gak pernah `addEntry()`, jadi sinyal short "hilang" dari archive; (2) `saveTriggerState({lastSentDate})` di akhir `main()` jalan TANPA SYARAT -- begitu SEMUA aset kebetulan window-bear+shortSignalSent di hari yang sama, `invalidNotes` kosong total (gerbang fallback "BELUM ADA SINYAL" ke-skip), gak ada SATU pesan pun terkirim, tapi trigger-state tetap ditandai "done" | `8fd1614` -- tambah `addEntry('sniper', ...)` di 3 titik short yang bolong; tambah flag `anyMessageSentToday` yang men-gate `saveTriggerState` (nol pesan terkirim = trigger-state SENGAJA gak ditandai done, cron berikutnya nyoba ulang) | `regressionTests.js` 17/17 tetap lolos (gak ada test baru spesifik -- perubahan murni pencatatan+gating, bukan logic kalkulasi; kalau kejadian lagi di masa depan, archive.json sekarang jadi bukti langsung) | ✅ CLOSED |
| BUG-KAELATRADE-0009 | 2026-09-14 | `setLeverage` di 4 titik Sniper (window-bear SHORT + entry utama + reopen-breakeven) gak pernah oper `positionType` -- leverage MEXC kesetel ke sisi LONG walau posisi yang dibuka SHORT | Ditemuin lewat audit kode PROAKTIF (bukan insiden) setelah tes live malam ini, mastiin jalur AUTO-SIGNAL konsisten sama primitive yang barusan diverifikasi manual. `mexcExecutor.js setLeverage(symbol, leverage, positionType=1)` default LONG kalau gak dioper -- caller Sniper gak pernah nyertain param ke-3 | `758fe2b` -- 4 titik Sniper + 1 titik Nyopet (buat konsisten, walau Nyopet Emas struktural LONG-ONLY jadi gak kena praktiknya) sekarang oper `positionType` eksplisit dari arah sinyal. Nyopet Emas TERBUKTI AMAN dari awal (audit ketauan dia sengaja gak pakai placeStopLoss/placeTakeProfit yang kena BUG-0007, pakai jalur polling+emergencyCloseMarket yang udah diverifikasi). **Susulan `c3d20e6`**: 5 ternary `direction==='buy'?X:Y` (versi ini + BUG-0007) DIKONSOLIDASI jadi 3 fungsi resmi export level-modul di `mexcExecutor.js` (`positionTypeFor`/`openSideFor`/`closeSideFor`) -- semua caller sekarang refer 1 sumber, gak ada lagi ternary tersebar yang bisa ke-skip/kebalik lagi | `regressionTests.js` test "mexcExecutor direction mapping" (buy->1/1/4, sell->2/3/2) sejak `c3d20e6` | ✅ CLOSED |

## Diarsipkan (superseded / gak relevan lagi)

_(kosong)_

## Cara Pakai

- **Bug baru ketemu**: tambah baris ke tabel "Aktif" -- ID naik urut, isi Root Cause SEJUJUR
  mungkin (termasuk kalau kita sendiri gak yakin 100% akar masalahnya), commit fix begitu ada.
- **Kalau bug ini juga perlu dilaporkan ke Olan/grup via WA** (kayak koreksi PnL 13 Sep) -- ikutin
  tetap [[feedback-wa-send-approval]] (draft + approval dulu), TAPI tutup pesannya dengan TTD role
  (13 Sep 2026, konsep NEXUS-FORGE): `— Kaela` baris baru `(laporan: 🐉 Drake · Debug Specialist)`.
  Kaela TETAP satu2nya pengirim -- ini co-signature nunjukin jenis laporan, bukan ganti identitas.
- **Bug tanpa regression test**: kolom "Regression Test" boleh `—` dulu, tapi kalau bug itu
  KRITIS (uang/data salah), prioritaskan tambahin test ke `regressionTests.js` (lihat pola
  BUG-KAELATRADE-0001 sbg contoh: fixture data + assert pakai ANGKA GROUND TRUTH dari insiden
  nyata, bukan angka rekaan).
- **JANGAN hapus baris** walau bug-nya udah lama/gak relevan lagi -- pindah ke bagian "Diarsipkan"
  kalau memang superseded (misal fitur yang kena bug udah dihapus total), biar histori tetap utuh.
