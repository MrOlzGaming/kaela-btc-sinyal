name: TEST (temp) - Onchain Metrics Reachability

on:
  workflow_dispatch: {}

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Test onchainMetrics.js dari runner (print-only, gak kirim WA)
        run: node onchainMetrics.js
